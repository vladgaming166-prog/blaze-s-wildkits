package com.blaze.wildkits.kit;

/**
 * Primary gear quality band for premium WildKits generation.
 * Netherite is always partial — never a full netherite set.
 */
public enum GearBand {
    IRON,
    DIAMOND,
    NETHERITE_PARTIAL
}
